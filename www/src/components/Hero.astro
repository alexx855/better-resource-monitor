---
---

<section class="hero">
  <div class="container">
    <header class="hero-content">
      <div class="badge">
        <span class="badge-text">v0.1.3 Available Now</span>
      </div>

      <h1 class="hero-title">
        Monitor your Silicon.<br />
        <span class="gradient-text">Zero Impact.</span>
      </h1>

      <p class="hero-subtitle">
        A native macOS menu bar system monitor built with Rust and Tauri.
        Experience precision monitoring with minimal resource consumption.
      </p>

      <div class="cta-group">
        <a href="https://github.com/alexx855/silicon-monitor/releases/latest" class="btn btn-primary">
          Download for macOS
          <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
            <path fill-rule="evenodd" clip-rule="evenodd" d="M8 14C11.3137 14 14 11.3137 14 8C14 4.68629 11.3137 2 8 2C4.68629 2 2 4.68629 2 8C2 11.3137 4.68629 14 8 14ZM8.75 4.5C8.75 4.08579 8.41421 3.75 8 3.75C7.58579 3.75 7.25 4.08579 7.25 4.5V9.43934L5.53033 7.71967C5.23744 7.42678 4.76256 7.42678 4.46967 7.71967C4.17678 8.01256 4.17678 8.48744 4.46967 8.78033L7.46967 11.7803C7.61032 11.921 7.80109 12 8 12C8.19891 12 8.38968 11.921 8.53033 11.7803L11.5303 8.78033C11.8232 8.48744 11.8232 8.01256 11.5303 7.71967C11.2374 7.42678 10.7626 7.42678 10.4697 7.71967L8.75 9.43934V4.5Z"/>
          </svg>
        </a>
        <a href="https://github.com/alexx855/silicon-monitor" class="btn btn-secondary">
          View Source
        </a>
      </div>

      <div class="stats-grid">
        <div class="stat-item">
          <span class="stat-value">&lt; 0.1%</span>
          <span class="stat-label">CPU Usage</span>
        </div>
        <div class="stat-item">
          <span class="stat-value">~15 MB</span>
          <span class="stat-label">Memory</span>
        </div>
        <div class="stat-item">
          <span class="stat-value">Native</span>
          <span class="stat-label">GPU Metrics</span>
        </div>
        <div class="stat-item">
          <span class="stat-value">MIT</span>
          <span class="stat-label">Open Source</span>
        </div>
      </div>
    </header>
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding: var(--space-4xl) 0;
    overflow: hidden;
    background: radial-gradient(circle at 50% 0%, #1c1c1e 0%, #000000 70%);
    border-bottom: 1px solid var(--border-subtle);
  }

  .container {
    max-width: var(--container-width-max);
    margin: 0 auto;
    padding: 0 var(--space-l);
    position: relative;
    z-index: 1;
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
  }

  /* Badge */
  .badge {
    display: inline-flex;
    align-items: center;
    padding: var(--space-2xs) var(--space-s);
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-full);
    margin-bottom: var(--space-l);
    backdrop-filter: blur(10px);
    transition: transform 0.2s ease;
  }

  .badge:hover {
    transform: translateY(-1px);
    background: rgba(255, 255, 255, 0.15);
  }

  .badge-text {
    font-size: 0.875rem;
    font-weight: var(--font-weight-medium);
    color: var(--accent-primary);
  }

  /* Typography */
  .hero-title {
    font-size: 3.5rem;
    line-height: var(--line-height-tight);
    font-weight: var(--font-weight-bold);
    letter-spacing: -0.02em;
    margin-bottom: var(--space-m);
    color: var(--text-primary);
  }

  .gradient-text {
    background: linear-gradient(135deg, #fff 0%, #8e8e93 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    display: inline-block;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    line-height: var(--line-height-relaxed);
    color: var(--text-secondary);
    max-width: 600px;
    margin-bottom: var(--space-2xl);
  }

  /* Buttons */
  .cta-group {
    display: flex;
    gap: var(--space-m);
    margin-bottom: var(--space-3xl);
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    padding: var(--space-s) var(--space-l);
    border-radius: var(--radius-full);
    font-weight: var(--font-weight-medium);
    font-size: 1rem;
    transition: all 0.2s cubic-bezier(0.2, 0.8, 0.2, 1);
    text-decoration: none;
  }

  .btn-primary {
    background: var(--color-white);
    color: var(--color-black);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 0 20px rgba(255, 255, 255, 0.3);
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.1);
    color: var(--color-white);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .btn-secondary:hover {
    background: rgba(255, 255, 255, 0.15);
    transform: translateY(-2px);
  }

  /* Stats Grid */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-2xl);
    width: 100%;
    padding-top: var(--space-2xl);
    border-top: 1px solid var(--border-subtle);
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    gap: var(--space-2xs);
  }

  .stat-value {
    font-size: 2rem;
    font-weight: var(--font-weight-bold);
    color: var(--text-primary);
    font-family: var(--font-family-mono);
    letter-spacing: -0.03em;
  }

  .stat-label {
    font-size: 0.875rem;
    color: var(--text-secondary);
    font-weight: var(--font-weight-medium);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero-title {
      font-size: 2.5rem;
    }

    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-l);
    }

    .cta-group {
      flex-direction: column;
      width: 100%;
      max-width: 300px;
    }

    .btn {
      justify-content: center;
      width: 100%;
    }
  }
</style>
